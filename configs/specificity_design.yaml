# Fig 3d (new) — specificity-aware DNA design
model:
  checkpoint: /data1/leihuang/project/TFScope/checkpoints/v19_combined_fm_deeppbs_contact/rag_seed42/ckpt_best.pt
parquet: data/processed/tf_pwm_aug_dbd.parquet
targets:            # target -> family (LHX5 primary)
  LHX5: Homeodomain
  MYOG: bHLH
  CREB3L2: bZIP
  ELK1: ETS
offtarget:
  n_per_target: 8          # hardest off-targets
  pool_per_family: 60      # candidates scored for similarity
design:
  length: 24
  gc_min: 0.35
  gc_max: 0.65
  max_homopolymer: 3
background:
  n_random: 50000
objective:
  bg: 0.25
  pseudocount: 0.001
  lambda_off: 1.0
  alpha_constraint: 10.0
ga:
  population: 600
  generations: 40
  elite_fraction: 0.05
  parent_fraction: 0.20
  mutation_rate: 0.04
  crossover_prob: 0.30
  seeds: 6
  final_designs: 20
  min_hamming: 5
seed: 42
